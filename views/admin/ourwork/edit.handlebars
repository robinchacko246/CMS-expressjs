<div class="container">


    <h3 class="alert alert-dark p-3 text-dark text-center">All ourwork </h3>


    <div class="row">
        <div class="col-6 mx-auto">
            <table class="table table-bordered text-center border-success text-white">
                <thead class="bg-primary">
                <tr>
                    <th>ourwork Title</th>
                    <th>ourwork Description</th>
                    <th>Action</th>
                </tr>
                </thead>
                <tbody class="table-hover bg-dark ourwork-list">
                {{#each ourworks}}
                    <tr>

                        <td>{{title}}</td>
                          <td>{{description}}</td>
                        <td class="d-flex justify-content-center">
                            <a href="/admin/ourwork/edit/{{_id}}" class="btn btn-sm btn-warning mr-2" role="button">Edit</a>
                            <form action="/admin/ourwork/{{_id}}?newMethod=DELETE" method="post">
                                {{!-- <button class="btn btn-sm btn-danger" type="submit">Delete</button> --}}
                            </form>
                        </td>
                    </tr>
                {{/each}}
                </tbody>

            </table>
        </div>
    </div>

    <hr>

    <div class="row p-4">
        <div class="col-md-8">
            <div class="form-group">
                <label for="title">Title</label>
                <input type="text" class="form-control" id="ourwork-title" name="title" value="{{ourwork.title}}">
                <label for="title">Description</label>
                <input type="text" class="form-control" id="ourwork-description" name="description" value="{{ourwork.description}}">
                <input type="hidden" class="form-control" id="ourwork-id" name="id" value="{{ourwork._id}}">
            </div>
            <button class="btn btn-outline-warning btn-lg" id="update-ourwork-button">Update ourwork</button>
        </div>
    </div>


</div>