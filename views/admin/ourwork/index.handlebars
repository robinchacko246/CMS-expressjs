<div class="container">


    <h3 class="alert alert-dark p-3 text-dark text-center">All ourwork </h3>


    <div class="row">
        <div class="col-6 mx-auto">
            <table class="table table-bordered text-center border-success text-white">
                <thead class="bg-primary">
                <tr>
                     <th>Service Title</th>
                    <th>Service Description</th>
                    <th>Service Action</th>
                </tr>
                </thead>
                <tbody class="table-hover bg-dark ourwork-list">
                {{#each ourwork}}
                    <tr>

                        <td>{{title}}</td>
                         <td>{{description}}</td>
                        <td class="d-flex justify-content-center">  
                            <a href="/admin/ourwork/edit/{{_id}}" class="btn btn-sm btn-warning mr-2" role="button">Edit</a>
                            <form action="/admin/ourwork/{{_id}}?newMethod=DELETE" method="post">
                                {{!-- <button class="btn btn-sm btn-danger" type="submit">Delete</button> --}}
                            </form>
                        </td>
                    </tr>
                {{/each}}
                </tbody>

            </table>
        </div>
    </div>

    <hr>

    <div class="row p-4">
        <div class="col-md-8">
                <div class="form-group">

                    <label for="title">Title</label>
                 
                    <input type="text" class="form-control" id="ourwork-title" name="title" placeholder="Enter Title">
                 <br>
                 <label for="title">Decription</label>
                    <textarea class="form-control" id="ourwork-decription" name="decription" placeholder="Enter Title"></textarea>
               
               
                </div>
                    <button class="btn btn-outline-success btn-lg" id="create-ourwork-button">Create ourwork</button>
        </div>
    </div>


</div>